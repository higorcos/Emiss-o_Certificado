<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eventos</title>
</head>
<body>
    <h3>Eventos abertos</h3>
        <div>
        <form id="form_eventSub" action="/user/redirect" method="POST">
        </div>
        <div>
            
            <input type='hidden' id="userMenu" name="userMenu" value="<%= idUser %>" />
            <button form="form_eventSub">Meus Eventos </button>
        </form>
    </div>
    </div>
    
        <br>
    <table>
        <thead>
        <tr>
            <th>Nome</th>
            <th>data</th>
            <th>horas</th>
            <th>Organização</th>
            <th>Inscrição</th>
        </tr>
        </thead>
        <tbody>
            <% result.forEach(function(event){ %>
                <tr>
                <th><%= event.name %></th>
                <th><%= event.date %></th>
                <th><%= event.hours %></th>
                <th><%= event.organization %></th>
                <th>
                <form id='idEvent'method='POST' action="/event/sub/<%= event.id %> "><input type='hidden' name='Event' value="<%= event.id%>">
                    <input type='hidden' id="userMenu" name="userMenu" value="<%= idUser %>" />
                <button> inscrever</button></button></form>
            </th>
                </tr>
            <%}) %> 
        </tbody>
    </table>
    
    <br>
</body>
</html>